# Dev-only dependencies for our own test suite (tools/tests/).
# tools/gov.py itself has ZERO runtime dependencies (stdlib only), by design:
# it must be vendorable into other teams' repos without a package install
# step, and a smaller dependency surface is easier to trust for a
# governance/integrity tool. See WRITEUP.md §3.
pytest>=8.0

# Optional: only needed to SIGN a manifest (gov.py keygen/seal) and to
# verify signatures. gov.py works without it -- hashes are stdlib -- but
# `verify` then reports "unsigned" rather than "ok". See govlib/integrity.py.
cryptography>=42.0
