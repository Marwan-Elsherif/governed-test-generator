This repository uses governed test generation. To turn a ticket into feature files, use the `feature-author` agent (or type `/run-ticket TKT-n`); it will guide the run through `python3 tools/gov.py`.

Files under `conventions/`, `shop/`, `tickets/`, `tools/`, `.github/`, `eval/` and `runs/` are governance material and are not edited in chat sessions.
